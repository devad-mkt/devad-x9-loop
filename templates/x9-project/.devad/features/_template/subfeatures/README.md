# Subfeatures

Create a subfolder only when the child has independent acceptance criteria,
owner, proof, or lifecycle. Otherwise keep it as a row in spec/TASKS.md.

Each child starts with TASK.md and uses the same smallest-linked-file rule.
