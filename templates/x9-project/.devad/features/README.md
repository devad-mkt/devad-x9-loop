# X9 Feature Map

Use features.index.json for routing. Copy _template for a new feature.

Read the feature TASK.md first. Then read only the smallest linked file needed.
Do not scan all features, subfeatures, specs, runs, or artifacts.
