# Project Model Policy

Package benchmark: devad-x9-suite-v3/benchmarks/model-routing.

| Task class | Default | Escalate |
| --- | --- | --- |
| Linx owner-facing bridge | gpt-5.6 high | Complex judgment routes to Thinx |
| Thinx normal planning/review | gpt-5.6 xhigh | Ultra for one very-hard pass, then return |
| Worker normal | gpt-5.6 high | xhigh when owner explicitly asks extra high |
| Reader/CHUNK | Lowest validated available | Mechanical extraction only; never decide |

Record project overrides and benchmark evidence below. Do not claim a permanent
winner before the promotion gate passes.

Track the live selection and confirmed return in MODEL_STATE.md.

Lower model/effort never means less owner context. Every role must pass the
owner-message and required-attachment receipt gate before acting.

The 2026-07-11 Sol/Luna results are pilots only. No lower Linx/Thinx/Worker
profile is auto-promoted until five real samples pass with zero critical errors
and the owner approves it. The explicit owner baseline above is binding.
